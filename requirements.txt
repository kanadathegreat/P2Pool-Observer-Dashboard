# requests: lets Python make web calls (talk to the p2pool.observer API)
requests==2.32.3

# PySide6: the GUI toolkit (Qt for Python). Same version used in the
# GUI Test folder, kept identical on purpose so we know it behaves
# the same way here.
PySide6==6.7.2

#Server admins prefered method of sharing data.
websockets
